import React from 'react'
import Grid from '../components/card/Grid'

interface ShopProps {

}

export const Shop: React.FC<ShopProps> = ({}) => {
    return (
        <div>
            <Grid />
        </div>
    );
}

export default Shop;